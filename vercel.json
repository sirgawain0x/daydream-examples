{
  "buildCommand": "cd .. && cd with-camera-input && pnpm install && pnpm run build && mkdir -p ../dist && cp -r dist/* ../dist/",
  "outputDirectory": "dist",
  "functions": {
    "api/**/*.js": {}
  }
}
